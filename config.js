module.exports = {
    secretKey: 'Ammar221',
    cookieOptions: {
      // Configure cookie options (e.g., secure, httpOnly, etc.) as needed
      // For example:
       secure: true,
       httpOnly: true,
    },
  };